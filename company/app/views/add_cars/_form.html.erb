<%= form_for(@add_car) do |f| %>
  <% if @add_car.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@add_car.errors.count, "error") %> prohibited this add_car from being saved:</h2>

      <ul>
      <% @add_car.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :make %><br>
    <%= f.text_area :make %>
  </div>
  <div class="field">
    <%= f.label :model %><br>
    <%= f.text_area :model %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
